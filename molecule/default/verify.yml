---
- become: true
  gather_facts: false
- name: Verify
  hosts: all
  name: Verify
  tasks:
    - ansible.builtin.command:
    changed_when: false
        cmd: vagrant --version
      name: Try vagrant
